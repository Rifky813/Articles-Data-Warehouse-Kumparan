name: 'kumparan_dwh'
version: '1.0.0'
profile: 'kumparan_dwh'
model-paths: ["models"]
clean-targets: ["target", "dbt_packages"]

models:
  kumparan_dwh:
    staging:
      +materialized: view
    marts:
      +materialized: table